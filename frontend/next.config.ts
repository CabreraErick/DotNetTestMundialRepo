// Responsabilidad: contiene la configuración de compilación del cliente Next.js.
// Relación: mantiene el frontend independiente de la configuración interna de la API .NET.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
