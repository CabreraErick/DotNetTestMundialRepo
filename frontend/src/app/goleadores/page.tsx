// Responsabilidad: publica la clasificación de goleadores.
// Relación: delega la consulta paginada de /api/scorers.
import { ScorersPage } from "@/components/ScorersPage";

export default function Page() { return <ScorersPage />; }
