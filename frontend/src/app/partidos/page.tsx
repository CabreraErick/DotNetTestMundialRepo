// Responsabilidad: publica la ruta del calendario y programación de partidos.
// Relación: delega la interacción al componente conectado con /api/matches.
import { MatchesPage } from "@/components/MatchesPage";

export default function Page() { return <MatchesPage />; }
