// Responsabilidad: publica la tabla automática de posiciones del torneo.
// Relación: delega la consulta paginada de /api/standings.
import { StandingsPage } from "@/components/StandingsPage";

export default function Page() { return <StandingsPage />; }
